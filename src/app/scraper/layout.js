export const metadata = {
  title: 'Import from URL',
  description: 'Turn a web page into Markdown and open it in Markdown Studio.',
};

export default function ScraperLayout({ children }) {
  return children;
}
